#include "main.h"
#include <string.h>

/**
 * _strlen - prints the length of a string
 *
 * @s: Memory address of the string
 *
 * Return: int(success)
 */
int _strlen(char *s)
{
	return (strlen(s));
}
